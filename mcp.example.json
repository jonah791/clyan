{
  "mcpServers": {
    "clyan": {
      "description": "AI-driven disk cleaner — scan, analyze, reclaim",
      "command": "python3",
      "args": ["-m", "clyan", "mcp"],
      "env": {},
      "disabled": false,
      "autoApprove": [
        "system_health",
        "scan_disk",
        "scan"
      ]
    }
  }
}
