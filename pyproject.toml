[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "clyan"
version = "0.1.0"
description = "AI-driven disk cleaner"
requires-python = ">=3.10"

[project.scripts]
clyan = "clyan.cli:main"
